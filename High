#include <stdio.h>
int main() {
    int num = 15; 
    for (int i = 5; i > 0; i--) { 
        for (int j = 0; j < i; j++) { 
            printf("%d ", num--);
        }
        printf("\n");
    }
    return 0;
}
